{
  "name": "nutrition-label-pal",
  "private": true,
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"cd backend && source venv/bin/activate && uvicorn app.main:app --reload --port 8000\" \"cd frontend && npm run dev\"",
    "test": "cd backend && venv/bin/python -m pytest tests/ -v",
    "build": "cd frontend && npm run build"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
